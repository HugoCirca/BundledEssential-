# Spawners

- Zombie $250, Skeleton $500 — **iron pickaxe or better** required (else cancelled)
- Right-click same-type to stack to 35x, keeps rate on break with hologram label
- Tune `spawner.*` (zombie-price, skeleton-price, max-multiplier) in `config.yml`
