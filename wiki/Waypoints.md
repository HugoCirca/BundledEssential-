# Waypoints

- `/waypoint` — open GUI (click to teleport)
- `/waypoint new <name>` — save current location
- `/waypoint delete <name>` — delete
- `/waypoint <name>` — teleport directly

Max 27 per player. Stored in `config.yml` per docs.
