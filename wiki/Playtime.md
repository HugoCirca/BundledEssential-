# Playtime

- `/playtime` — yours; `/playtime <player>` — other (offline ok); `/playtime leaderboard|top` — top 10
- `/playtime optout` — silent farm to personal vault (no messages); `optin` — claim vault; `vault` — show vault
