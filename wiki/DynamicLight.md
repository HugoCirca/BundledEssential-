# Dynamic Light

No command — hold light in either hand → client-side `Light` via `sendBlockChange` at feet/eye, no real block placed so crouch/buckets never break. Freezes near spawners (`spawner-freeze-radius`). Tune `interval-ticks` in `config.yml`.
