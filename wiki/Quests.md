# Quests & Daily

- `/quest` — progress; `claim` — collect + instant next; `skip` — fresh roll
- 15 tasks $20-90; login streak `base+(N-1)*per-day+random` + weekly $50 + monthly $200; placed shop blocks don't count
- Tune `rewards.*` in `config.yml`
